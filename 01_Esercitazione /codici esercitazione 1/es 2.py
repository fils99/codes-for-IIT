import numpy as np
import math
import matplotlib.pylab as plt
import random
casuali=[]
for i in range(0,10):
    d=random.randint(-100,100)/100
    casuali.append(d)
massimo=np.max(casuali)
minimo=np.min(casuali)
assoluti=np.abs(casuali)
max_ass=np.max(assoluti)
min_ass=np.min(assoluti)
somma=np.sum(casuali)
somma_ass=np.sum(assoluti)



               
               
               

